@page "/counter"
@* @using Refit *@
@* @inject HttpClient HttpClient *@
@inject IExternalAPI myApi

<h1>Counter</h1>

<p role="status">Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>
<button class="btn btn-secondary" @onclick="APITest">Click me</button>

@code {
    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount++;
    }

    private async void APITest()
    {
        // var myApi = RestService.For<IExternalAPI>(HttpClient);
        // var products = await myApi.GetProductById(2);

        var product = await myApi.GetProductById(1);
    }
}
